import { City } from './city';
export class Flight {
  price: number;
  seats: number;
  origin: City;
  destination: City;
  flightNumber: string;
  departureTime: string;
  arrivalTime: string;
  departureDate: string;
  arrivalDate: string;
}
